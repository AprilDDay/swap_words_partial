package ie.gmit.dip;

import java.util.TreeSet;
import java.util.TreeMap;
/*
public class SearchForWords extends Database{
	
	private String googleWord;
	private String word;
		
	public SearchForWords(TreeMap<String, String> map, TreeSet<String> set) {
		super(map, set);
		this.googleWord = googleWord;
		this.googleWord = word;
	}
	

//shouldn't be in one big method	
//for a query
public String getGoogleEquivalent(String word) {
	if (getMap().containsKey(word)){
		return getMap().get(word);
	}else {
		return word;
	}	
}

}
*/